CREATE TABLE [dbo].[CodeObjects] (
    [Id]    INT            IDENTITY (1, 1) NOT NULL,
    [Code]  INT            NOT NULL,
    [Value] NVARCHAR (MAX) NULL,
    CONSTRAINT [PK_CodeObjects] PRIMARY KEY CLUSTERED ([Id] ASC)
);